package de.hsmannheim.ss2016.tpe.dell.grp1.uebung2.crypter;

/**
 * Enum of all Crypters
 * <p>
 * returns a new Crypter so that a new crypter of the same type
 * does not overwrite the old one
 *
 * @Author TPE_GRP_1
 */
enum CrypterEnum {
    CAESAR(), SUB(), XOR()
}
